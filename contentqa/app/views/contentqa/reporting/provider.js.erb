<% @reports.each do |type, report| %>
  $('.<%= type %>_status').html("<%= j(render "report_status", :report => report, :ingest_id => @ingest['_id'], :type => type) %>");
<% end %>

<% if @all_reports %>
    location.reload();
<% end %>
